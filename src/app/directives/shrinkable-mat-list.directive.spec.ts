import { ShrinkableMatListDirective } from './shrinkable-mat-list.directive';

describe('ShrinkableMatListDirective', () => {
  it('should create an instance', () => {
    const directive = new ShrinkableMatListDirective();
    expect(directive).toBeTruthy();
  });
});
